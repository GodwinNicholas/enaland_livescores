
// removes duplicates from array of objects
function removeDuplicates(arr) {
    let noDuplicates = [];
    arr.forEach(el => {
        if (!el in noDuplicates) {

        }
    })
}

module.exports = removeDuplicates